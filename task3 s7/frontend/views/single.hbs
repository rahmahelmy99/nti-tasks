{{>header}}
{{>navbar}}
<div class="border border-3 border-primary p-3">
    {{#if task}}
        {{task.title}}<br>
        {{task.name}}
        
    {{else}}
        task not found
    {{/if}}
</div>
{{>footer}} 